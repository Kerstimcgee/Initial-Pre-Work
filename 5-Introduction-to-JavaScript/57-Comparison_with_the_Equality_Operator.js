//we learned to use the "equivalent" equals sign to set up our true
//or false conditional

// Setup
function testEqual(val) {
  if (val==12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
