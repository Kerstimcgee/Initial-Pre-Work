//we learned we can use floor to bring a number to nearest whole number and
//then how to generate a random decimal number, then convert int
// to it's whole (*20) 

var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.

  return Math.floor(Math.random() * 10);
}
