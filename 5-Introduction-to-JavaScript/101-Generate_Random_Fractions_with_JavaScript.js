//learned to generate random dec. number with Math.random function,
//but set it so that it generates one only if larger than 0 

function randomFraction() {

  // Only change code below this line.
    var result = 0;
    while (result === 0) {
    result = Math.random();
  }
return result;

  // Only change code above this line.
}
